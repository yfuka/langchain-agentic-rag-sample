# Copy this file to `.env` and fill in the values.
# The app loads these via python-dotenv in `agentic_rag.py`.

# OpenAI Endpoint
OPENAI_ENDPOINT=

# OpenAI API key (starts with sk- or sk-proj-)
OPENAI_API_KEY=

# Chat completion model for the agent (e.g., gpt-4o, gpt-4.1, o3-mini)
OPENAI_MODEL=

# Embedding model used for vector store
OPENAI_EMBEDDING_MODEL=
